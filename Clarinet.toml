[project]
name = "create-yield"
authors = ["Create Yield Team"]
description = "A decentralized yield generation and investment platform"
telemetry = false
requirements = []
[contracts.yield-dao]
path = "contracts/yield-dao.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
